# Environment Variables for linkblog-email-connector
# Copy this file to .env or configure these in your Cloudflare Workers environment

# The email address that is allowed to send link posts
# This should be a trusted email address that you control
LINKS_ALLOWED_SENDER=your-email@example.com

# GitHub Personal Access Token with repo permissions
# Generate at: https://github.com/settings/tokens
# Required permissions: repo (full control of private repositories)
GITHUB_TOKEN=your_github_token_here

# GitHub repository name (configured in wrangler.toml)
GITHUB_REPO=your-username.github.io

# GitHub username (configured in wrangler.toml)
GITHUB_USER=your-username